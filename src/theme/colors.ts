interface ColorsType {
  primary: string;
  accent: string;
  white: string;
  black: string;
}

const colors: ColorsType = {
  primary: '#F7EADE',
  accent: '#3D6343',
  white: '#ffffff',
  black: '#000000',
};

export default colors;
