interface ColorsType {
  primary: string;
  secondary: string;
  white: string;
  black: string;
}

const colors: ColorsType = {
  primary: '#ffffff',
  secondary: '#000000',
  white: '#ffffff',
  black: '#000000',
};

export default colors;
